lalanguage: generic

sudo: required

addons:
  apt:
    packages:
      - libcurl4-openssl-dev
      - libelf-dev
      - libdw-dev
      - cmake

after_success: |
  kcov coverage script.sh &&
  bash <(curl -s https://codecov.io/bash)
